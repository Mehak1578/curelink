# CureLink Frontend (Starter)

This is a Vite + React starter app with Tailwind CSS. It contains basic auth pages that call the backend API.

Run locally:

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

Open http://localhost:5173
